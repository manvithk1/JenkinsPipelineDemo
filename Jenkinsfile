stage('build') {
    // some block
    // example of using arguments to a script
    echo 'Building'
}
stage('lightRailtest') {
    // some block
    // example of using arguments to a script
    echo 'Running lightRailtests'
}
stage('SalinaTests') {
    // some block
    // example of using arguments to a script
    echo 'Running SalinaTests'
}
