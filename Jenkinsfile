stage('build') {
    // some block
    // example of using arguments to a script
    echo 'Building'
    bat 'Batchfile'
}
stage('lightRailtest') {
    // some block
    // example of using arguments to a script
    echo 'Running lightRailtests'
}
stage('SalinaTests') {
    // some block
    // example of using arguments to a script
    echo 'Running SalinaTests'
}
